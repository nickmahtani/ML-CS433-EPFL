- scipy is a library that makes calulating pairwise distances super efficicient
- tensor brodcasting uses numpy magic to make tensor broadcasting super efficient

**Task C**

- implement the assignment of data to two given distributions
- determinant is a single number that tells you how big the matrix spreads space
    - big determinant, data is spread out widely
    - small determinant, data is tight, concentrated
    - measures size/area of the spread
- covariance matrix → defines shape and orientation of the spread
    - captures how the variances changes in different directions
    - interdimensional dependencies for variance
    - the diagonals of the variance matrix are the variances
- density model is a formula that gives the likelihood (how well a model explains a point) → describes how data is spread